import { StyleSheet } from "react-native";

export default StyleSheet.create({
  containerStyle: {
    borderWidth: 1,
    borderColor: "#ddd",
    padding: 10,
    fontSize: 18,
    borderRadius: 6,
  },
});
